# Cloudflare Workers Builds entry for repo waelzaid66-max/bancoo.
# SoT production host remains Coolify — see deploy/coolify + docker-compose.coolify.yml.
# This file unblocks the GitHub check "Workers Builds: bancoo" which otherwise fails
# with no wrangler entrypoint on every push (including docs-only PRs).

name = "bancoo"
main = "deploy/cloudflare/stub-worker.ts"
compatibility_date = "2025-07-01"
workers_dev = true

# No routes / custom domains — stub must not capture banco production hostnames.
